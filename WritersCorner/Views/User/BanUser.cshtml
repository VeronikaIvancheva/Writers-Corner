@model UserViewModel

@{
    ViewData["Title"] = "BanUser";
}

<h1>BanUser</h1>

<div class="modal" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            @*<div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Create a new theatre</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>*@
            <div class="modal-body">
                <form enctype="multipart/form-data" asp-area="Administrator" asp-controller="User" asp-action="BanUser">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="BanDays" class="control-label"></label>
                        <input asp-for="BanDays" class="form-control" />
                        <span asp-validation-for="BanDays" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="BanReason" class="control-label"></label>
                        <input asp-for="BanReason" class="form-control" />
                        <span asp-validation-for="BanReason" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <input type="submit" value="Create" class="btn btn-primary" />
                        <button type="reset" value="Reset" class="btn btn-secondary">Reset</button>
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>